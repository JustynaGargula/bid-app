
<!DOCTYPE html>
<html>
    <head>
        <title>Przetargi</title>
        <%- include('../common/head-common') %>
    </head>
        <body>
        <%- include('../common/navbar') %>
        <div class="main-content">
            <h1>Szczegóły przetargu</h1>
            <%- include('../common/tender-details-div') %>
        </div>
        <% if (finish_time > current_time) { %>
            <div class="centered-text">
                <button class="btn submit-button" onclick="window.location.href='/tenders-list/<%= id %>/new-bid';">Złóż ofertę</button>
            </div>
        <% } else {%>
            <h1>Złożone oferty</h1>
        <% } %>
        <%- include('../common/body-common') %>
    </body>
</html>
